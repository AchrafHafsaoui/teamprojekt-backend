##### STILL UNDER WORK

class Bus:

    def __init__(self, license_plate: str, date_of_purchase: str, status: str, 
                 state_of_charge: float, electric_capacity: float, seat_capacity: int):
        self.license_plate = license_plate
        self.date_of_purchase = date_of_purchase
        self.status = status
        self.state_of_charge = state_of_charge
        self.electric_capacity = electric_capacity
        self.seat_capacity = seat_capacity

    def AddBus():
        # get the database and write to it
        print("New bus added")

    def DeleteBus():
        # get the database and remove from it
        print("Bus deleted")

    # fetch the bus by a dertain param
    def FetchBus(param):
        # get the database and..
            # if licenseplate
            # elif dateofpurchase
            # elif status
            # stateofcharge
            # ..
        print("Bus fetched:")


    
    
